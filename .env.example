# Copy this file to .env and update the values for your local Postgres setup.

# For local development (PostgreSQL installed via Homebrew, etc.):
URL_DB=jdbc:postgresql://localhost:5432/mydatabase
DB_USER=your_mac_username
DB_PASS=

# For Docker Compose, these are already set in compose.yaml.
